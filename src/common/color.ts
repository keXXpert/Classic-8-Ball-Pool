
export enum Color {
    white = '#BABABA',
    black = '#2B2B2B',
    red = '#B62A2F',
    yellow = '#FECA34'
}

// added ball type enum
export enum BallTypeEnum {
    Unknown, Cue, Stripe, Solid, Eight, Red, Color
}